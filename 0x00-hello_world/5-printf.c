#include <stdio.h>
/**
 * main - start of the program
 * Return: always returns 0 meaning successful termination
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
